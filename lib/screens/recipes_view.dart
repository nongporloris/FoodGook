import 'package:flutter/material.dart';

class RecipesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("recipesView"),
      ),
    );
  }
}
