import 'package:flutter/material.dart';

class ForumView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ForumView"),
      ),
    );
  }
}
