import 'package:flutter/material.dart';

class GroceryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("GroceryView"),
      ),
    );
  }
}
